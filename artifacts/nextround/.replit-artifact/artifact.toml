kind = "web"
previewPath = "/"
title = "NextRound"
version = "1.0.0"
id = "artifacts/nextround"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22643

[services.development]
run = "pnpm --filter @workspace/nextround run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nextround", "run", "build" ]
serve = "static"
publicDir = "artifacts/nextround/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22643"
BASE_PATH = "/"
